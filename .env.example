# ===========================================
# FastAPI + Cognito + PostgreSQL Configuration
# ===========================================

# Database Configuration
# For local development with Docker Compose
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/fastapi_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fastapi_db

# Security Settings (CHANGE IN PRODUCTION!)
# Generate with: openssl rand -hex 32
SECRET_KEY=6a4ce3172f61d9607bb9b4bc5016896d2a884e1f1f975c942daa440e1a877579
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application Environment
ENVIRONMENT=development
DEBUG=true

# AWS Configuration
AWS_REGION=ap-south-1

# AWS Cognito Configuration (Auto-populated by deploy script)
COGNITO_USER_POOL_ID=
COGNITO_CLIENT_ID=
USE_COGNITO=false

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Settings (adjust for production)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
ALLOWED_HEADERS=*